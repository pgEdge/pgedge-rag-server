version: "2"

linters:
    default: standard
    exclusions:
        presets:
            - comments
            - std-error-handling
            - common-false-positives
        rules:
            - linters: [staticcheck]
              text: "ST1005"

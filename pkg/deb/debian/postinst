#!/bin/sh
set -e

# debhelper emits its own systemd-tmpfiles call too; both are idempotent.
if [ "$1" = "configure" ]; then
    systemd-tmpfiles --create /usr/lib/tmpfiles.d/pgedge-rag-server2.conf >/dev/null 2>&1 || true
fi

#DEBHELPER#

exit 0

# ============================================================
# pgEdge RAG Server Demo - Environment Configuration
# ============================================================
# Copy this file to .env and fill in your API keys:
#   cp .env.example .env
#
# Then edit .env with your API keys and run:
#   docker compose up -d
#
# Or use the interactive setup script:
#   ./pgedge-rag-demo.sh
# ============================================================

# ------------------------------------------------------------
# Docker Compose Project Name
# ------------------------------------------------------------
COMPOSE_PROJECT_NAME=pgedge-rag-demo

# ------------------------------------------------------------
# REQUIRED: LLM API Keys
# ------------------------------------------------------------
# At least one API key is required for embeddings.
# Get your OpenAI key at: https://platform.openai.com/api-keys

OPENAI_API_KEY=

# Optional: For Anthropic Claude completions
# Get your key at: https://console.anthropic.com/settings/keys

ANTHROPIC_API_KEY=

# ------------------------------------------------------------
# Optional: Service Ports
# ------------------------------------------------------------
# Customize service ports if defaults conflict with other services

WEB_CLIENT_PORT=3001
RAG_SERVER_PORT=8081
POSTGRES_PORT=5433

# ------------------------------------------------------------
# Demo Database Credentials
# ------------------------------------------------------------
# These credentials are pre-configured for the demo database.
# DO NOT change these unless you also update docker-compose.yml
# and the inlined RAG server configuration

POSTGRES_DB=ragdb
POSTGRES_USER=docuser
POSTGRES_PASSWORD=docpass
